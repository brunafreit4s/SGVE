<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <environment names="Development">
        <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.css" />
        <link rel="stylesheet" href="~/css/Login.css" />
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-0evHe/X+R7YkIZDRvuzKMRqM+OrBnVFBL6DOitfPri4tjfHxaWutUpFmBp4vmVor" crossorigin="anonymous">
        <script src="https://kit.fontawesome.com/4a127b145c.js" crossorigin="anonymous"></script>
        <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    </environment>

    <title>@ViewBag.Title</title>

</head>
<body>
    @{
        ViewData["Title"] = "Painel de Acesso";
    }

    <header>
        <div class="container">
            <nav class="navbar bg-light fixed-top border-bottom">
                <div class="container-fluid">
                    <a class="navbar-brand" href="#"><span class="navbar-text text-primary h2">SGVE - Sistema Gerenciador de Vendas e Estoque</span></a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasNavbar" aria-controls="offcanvasNavbar">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="offcanvas offcanvas-end" tabindex="-1" id="offcanvasNavbar" aria-labelledby="offcanvasNavbarLabel">
                        <div class="offcanvas-header">
                            <h5 class="offcanvas-title text-primary h1" id="offcanvasNavbarLabel">Áreas de acesso:</h5>
                            <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
                        </div>
                        <div class="row col-lg-12 offcanvas-body">
                            <ul class="navbar-nav justify-content-end form-group-1 pe-3">
                                @if (ViewBag.Codigo == 3)
                                {
                                    <li class="row col-lg-12 nav-item dropdown">
                                        <a class="nav-link dropdown-toggler" href="#" id="offcanvasNavbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                            Funcionário
                                        </a>
                                        <ul class="dropdown-menu img-responsive" aria-labelledby="offcanvasNavbarDropdown">
                                            <li><a class="dropdown-item col-lg-12" href="@Url.Action("Cadastrar", "Funcionario")"><i class="fa-solid fa-folder-plus"></i> Cadastrar</a></li>
                                            <li><hr class="dropdown-divider col-lg-12"><a class="dropdown-item" href="@Url.Action("Consultar", "Funcionario")"><i class="fa-solid fa-magnifying-glass"></i> Pesquisar</a></li>
                                            <li></li>
                                        </ul>
                                    </li>
                                }
                                @if (ViewBag.Codigo == 3 || ViewBag.Codigo == 4)
                                {
                                    <li class="row col-lg-12 nav-item dropdown">
                                        <a class="nav-link dropdown-toggler" href="#" id="offcanvasNavbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                            Produto
                                        </a>
                                        <ul class="dropdown-menu img-responsive" aria-labelledby="offcanvasNavbarDropdown">
                                            <li><a class="dropdown-item col-lg-12" href="@Url.Action("Cadastrar", "Produto")"><i class="fa-solid fa-file-circle-plus"></i> Incluir</a></li>
                                            <li><hr class="dropdown-divider col-lg-12"><a class="dropdown-item" href="@Url.Action("Consultar", "Produto")"><i class="fa-solid fa-magnifying-glass"></i> Pesquisar</a></li>
                                            <li></li>
                                        </ul>
                                    </li>
                                    <li class="row col-lg-12 nav-item dropdown">
                                        <a class="nav-link dropdown-toggler" href="#" id="offcanvasNavbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                            Estoque
                                        </a>
                                        <ul class="dropdown-menu img-responsive" aria-labelledby="offcanvasNavbarDropdown">
                                            <li><a class="dropdown-item" href="@Url.Action("Consultar", "Estoque")"><i class="fa-solid fa-file-circle-check"></i> Consultar</a></li>
                                            <li></li>
                                        </ul>
                                    </li>
                                }
                                @if (ViewBag.Codigo == 3 || ViewBag.Codigo == 4 || ViewBag.Codigo == 2)
                                {
                                    <li class="row col-lg-12 nav-item dropdown">
                                        <a class="nav-link dropdown-toggler" href="#" id="offcanvasNavbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                            Venda
                                        </a>
                                        <ul class="dropdown-menu img-responsive" aria-labelledby="offcanvasNavbarDropdown">
                                            <li><a class="dropdown-item" href="@Url.Action("Consultar", "Funcionario")"><i class="fa-solid fa-cart-plus"></i> Nova Venda</a></li>
                                            <li></li>
                                        </ul>
                                    </li>
                                }
                            </ul>
                        </div>
                    </div>
                </div>
            </nav>
        </div>
    </header>

    <div class="container">
        <div class="col-lg-12">
            <h1 class="display-4">Bem Vindo(a)</h1>
            <h3>@ViewBag.NomeLogado</h3>
            <hr />
            <br />
            <h3>@ViewBag.Title</h3>
            <p>
                <h4 class="text-primary"><b>Informações gerais do sistema. </b></h4>
            </p>
            <hr />
            <br />
            <div class="row col-lg-12">
                <div class="card form-group col-lg-6 divProdutos"><h3 class="text-info text-center"><br />Produtos</h3><div id="chart_divProdutos"></div></div>
                <div class="col-lg-1"></div>
                <div class="card form-group col-lg-5">
                    <br />
                    <div class="row col-lg-12 form-group">
                        <label class="h4 text-info">Total de Funcionários:</label>
                        <label class="h4 text-success"></label>
                    </div>
                    <div class="row col-lg-12 form-group">
                        <label class="h4 text-info">Total de Fornecedores:</label>
                        <label class="h4 text-success"></label>
                    </div>
                    <div class="row col-lg-12 form-group">
                        <label class="h4 text-info">Total de Produtos:</label>
                        <label class="h4 text-success"></label>
                    </div>
                    <div class="row col-lg-12 form-group">
                        <label class="h4 text-info">Total de Vendas:</label>
                        <label class="h4 text-success"></label>
                    </div>
                </div>
            </div>

            <div class="row col-lg-12">
                <div class="card form-group divVendas"><h3 class="text-info text-center"><br />Vendas</h3><div id="chart_divVendas"></div></div>
            </div>
        </div>
    </div>

    <footer class="bg-light border-top footer text-muted">
        <div class="container">
            <div class="form-group">
                <p class="h4">
                    SGVE - Sistema Gerenciador de Vendas e Estoque
                </p>
            </div>
        </div>
    </footer>
    <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/js/bootstrap.bundle.min.js" integrity="sha384-pprn3073KE6tl6bjs2QrFaJGz5/SUsLqktiwsUTF55Jfv3qYSDhgCecCxMW52nD2" crossorigin="anonymous"></script>
</body>
</html>

<script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
<script type="text/javascript">
    var vLista_Produtos = @Html.Raw(Json.Serialize(ViewBag.Lista_Produtos));

    google.charts.load('current', { 'packages': ['corechart'] });
    google.charts.setOnLoadCallback(drawChart_Produtos);

    function drawChart_Produtos() {
        if (vLista_Produtos != null) {
            var data = new google.visualization.DataTable();
            data.addColumn('string', 'Topping');
            data.addColumn('number', 'Slices');
            vLista_Produtos.forEach(function (item) {
                data.addRows([
                    [item.nomE_PROD, item.quantidadE_PROD],
                ]);
            });

            var options = {
                'title': 'Quantidade de Produtos disponíveis em Estoque:',
                'width': 500,
                'height': 400
            };

            var chart = new google.visualization.PieChart(document.getElementById('chart_divProdutos'));
            chart.draw(data, options);
        }
    }

    var vLista_Vendas = @Html.Raw(Json.Serialize(ViewBag.Lista_Vendas));

    google.charts.load('current', { 'packages': ['bar'] });
    google.charts.setOnLoadCallback(drawChart_Vendas);

    function drawChart_Vendas() {
        if (vLista_Vendas != null) {
        console.log(vLista_Vendas);
            var data = new google.visualization.DataTable();
            data.addColumn('string', 'Data da Venda');
            data.addColumn('number', 'Total');
            vLista_Vendas.forEach(function (item) {
                data.addRows([
                    [item.datA_VENDA, item.totaL_VENDA],
                ]);
            });

            var options = {
                'title': 'Total de Vendas realizadas até o momento:',
                'width': 400,
                'height': 300
            };

            var chart = new google.charts.Bar(document.getElementById('chart_divVendas'));
            chart.draw(data, options);
        }
    }
</script>